# service 基础环境变量默认值。
# 服务监听端口，默认 3000。
SERVICE_PORT=3000
# Vite 开发服务端口，默认 3100。
FRONTEND_PORT=3100
# 日志级别，默认 `log`。
LOG_LEVEL=log
# 日志文件前缀，默认 `service`。
LOG_FILE_PREFIX=service
# 是否同时输出日志到控制台，默认 `false`。
LOG_TO_CONSOLE=false
# 是否启用全局限流，默认 `false`。
ENABLE_THROTTLE=false
