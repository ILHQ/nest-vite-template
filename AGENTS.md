# AGENTS.md

## 默认响应

- 始终使用中文简体回复。
- 修改代码前必须先读取相关文件，确认现有结构与约束后再实现。
- 未经用户主动要求，不要计划或执行 `git commit`、`git push`、创建分支等 Git 操作。

## 默认技能

- 本目录任务默认使用项目根目录 `.codex/skills/` 下的技能作为技能来源。
- 本文件位于项目根目录，因此技能目录相对路径为 `.codex/skills/`。
- 当用户显式点名技能，或任务符合某个 `SKILL.md` 的 `description` 时，必须先读取并遵循对应技能。
- 多个技能同时匹配时，只使用完成任务所需的最小技能集合，并按任务主线决定顺序。
- 读取技能时优先读取匹配技能的 `SKILL.md`；只有被 `SKILL.md` 明确引用且当前任务需要时，才继续读取 `references/` 等附加文件。

## 当前默认技能清单

- `frontend`：用于 `frontend/` 下 React 18、JS/TS/TSX、组件逻辑、hooks、路由、请求、工具函数、常量与数据处理。
- `web-style`：用于 `frontend/` 下 Less/CSS、class、页面布局、视觉样式、Antd 样式覆盖与主题 token。
- `pixso-mcp`：用于 Pixso 设计稿读取、节点/标注/切图解析，并落地为 React + Antd + Less 页面。

## 技能选择规则

- 任务核心是组件结构、业务逻辑、状态流转、接口调用、路由或工具代码时，优先使用 `frontend`。
- 任务核心是样式、布局、间距、颜色、圆角、阴影、滚动区域或主题 token 时，优先使用 `web-style`。
- 用户提供 Pixso 链接，或明确要求按 Pixso 设计稿开发页面时，优先使用 `pixso-mcp`，并结合 `frontend` 与 `web-style` 的约束落地。

## 工程原则

- KISS：优先选择直接、清晰、可维护的实现。
- YAGNI：只实现当前明确需要的能力，不预留未确认功能。
- DRY：重复逻辑达到 3 次及以上必须抽取复用。
- SOLID：保持职责清晰，避免把组件、工具函数或样式职责混在一起。
