interface:
  display_name: "Frontend"
  short_description: "frontend目录 JS/TS/TSX 组件逻辑与路由实现规范"
  default_prompt: "Use $frontend 仅在 frontend 目录下实现页面组件、业务逻辑、路由、hooks、工具函数或常量，不处理 Less/CSS 样式。"
policy:
  allow_implicit_invocation: true
